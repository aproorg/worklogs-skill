# worklogs-v2 Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-19

## Active Technologies

- Bash 5.x (retrieval scripts), Markdown (skill definition) + curl, jq, MCP servers (Notion via `mcp.notion.com`, Jira via `mcp.atlassian.com`), LiteLLM (estimation reasoning) (001-billable-worklog-skill)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Bash 5.x (retrieval scripts), Markdown (skill definition)

## Code Style

Bash 5.x (retrieval scripts), Markdown (skill definition): Follow standard conventions

## Recent Changes

- 001-billable-worklog-skill: Added Bash 5.x (retrieval scripts), Markdown (skill definition) + curl, jq, MCP servers (Notion via `mcp.notion.com`, Jira via `mcp.atlassian.com`), LiteLLM (estimation reasoning)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
